# Screenshot Assets

Browser-captured Admin UI screenshots for the VitePress docs live here.

Rules:

- Use demo data only.
- Do not capture real tokens, passwords, emails, database URLs, or secrets.
- Name files by route and state, for example `admin-schema-builder-empty.png`.
- Use `-annotated.png` for copies with arrows or labels.
